# Handwritten Digit Recognizer
A deep learning app that recognizes handwritten digits using a CNN trained on MNIST.

## Demo
(add screenshot or gif here)

## Run Locally
pip install -r requirements.txt
streamlit run app.py

## Accuracy
~95-96% on MNIST test set